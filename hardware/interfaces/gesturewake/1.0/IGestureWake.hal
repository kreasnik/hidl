package vendor.hct.hardware.gesturewake@1.0;

import IGestureWakeCallback;
interface IGestureWake {
    startGesture(bool state) generates (int32_t result);
    oneway readGesture(IGestureWakeCallback cb);
};

